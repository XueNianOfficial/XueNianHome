# 预设AI自主画图功能 - 修改说明

## 修改日期
2026-08-16

## 修改内容

### 功能概述
为每个AI预设添加独立的AI自主画图功能开关和画图引导语配置，允许不同的预设控制AI是否可以在对话中自主创作插画，以及指导AI的画图风格。

---

## 核心功能

### 什么是AI自主画图？

AI在对话过程中，可以主动判断何时需要配图，并通过 `[DRAW]` 标记协议自主创作插画插入到对话中。

**标记协议示例**：
- `[DRAW]一只可爱的小狐狸在雪地里玩耍[/DRAW]` - 普通画图
- `[DRAW:ref=xn]雪年在咖啡厅喝咖啡[/DRAW]` - 画角色自己，使用立绘参考
- `[DRAW:ref=img]模仿用户图片的风格画一幅画[/DRAW]` - 参考用户历史图片

### 预设级别的控制

每个预设现在可以独立配置：

1. **AI自主画图开关** - 决定该预设下AI能否使用 `[DRAW]` 标记自主画图
2. **画图引导语** - 注入到系统提示词中，指导AI的画图风格和创作方向

---

## 详细修改

### 1. 服务端类型定义更新

#### 文件：`server/utils/settings.ts`

**新增字段到 `AISettingsPreset` 接口**：
- `supportsImageGen?: boolean` - 是否允许AI自主画图
- `imageGenPrompt?: string` - AI画图引导语（注入到系统提示词）

---

### 2. 系统提示词注入逻辑更新

#### 文件：`server/utils/ai.ts`

**`buildFullMessages` 函数修改**：

```typescript
// 检查预设是否启用画图功能
const preset = presetName ? settings.presets.find(p => p.name === presetName) : undefined
const shouldEnableDraw = preset?.supportsImageGen ?? !!settings.imageGen?.apiKey

// 预设启用画图功能才注入标记协议
if (shouldEnableDraw) {
  // 如果预设配置了自定义画图引导语，则追加到协议说明中
  const customDrawPrompt = preset?.imageGenPrompt?.trim()
  const drawInstruction = customDrawPrompt
    ? DRAW_CAPABILITY_INSTRUCTION + `\n\n【画图风格引导】${customDrawPrompt}`
    : DRAW_CAPABILITY_INSTRUCTION
  systemPromptContent += drawInstruction
}
```

**逻辑说明**：
1. 检查预设是否开启了 `supportsImageGen`
2. 开启时在系统提示词末尾注入 `[DRAW]` 标记协议说明
3. 如果配置了自定义引导语，追加到系统提示词中
4. 引导语指导AI在什么情况下画图、使用什么风格

---

### 3. 管理后台界面更新

#### 文件：`app/components/admin/AdminAISettings.vue`

**新增UI元素**：

1. **预设能力复选框区域**：
   - 复选框标签：`🎨 AI自主画图`
   - 启用后显示AI画图引导语配置区域

2. **AI画图引导语配置区域**：
   - 标签：`AI画图引导语`
   - 3行文本框，用于输入引导语
   - 占位符：`注入到系统提示词的画图风格引导，例如：画图时请使用二次元插画风格，注重细节和色彩`
   - 提示说明：
     > 开启AI自主画图后，AI可以在对话中用[DRAW]标记自主创作插画。此处填写的引导语会追加到系统提示词中，用于指导AI的画图风格。留空则使用默认引导。

3. **预设列表折叠头部**：
   - 在能力图标区域显示 🎨 图标（启用AI自主画图时）

---

### 4. 前端类型定义更新

#### 文件：`app/types/index.ts`

**`ChatPreset` 接口新增字段**：
- `supportsImageGen?: boolean` - 是否支持AI自主画图
- `imageGenPrompt?: string` - AI画图引导语

#### 文件：`server/api/presets.get.ts`

**返回数据包含画图配置**（供前端显示使用）

---

## 使用场景示例

### 场景1：启用AI自主画图，无自定义引导

```yaml
预设名称：通用助手
AI自主画图：✓
画图引导语：（留空）
```

**效果**：
- AI可以在对话中使用 `[DRAW]` 标记自主画图
- 使用默认的标记协议说明
- AI会根据对话情境自主决定何时画图

**对话示例**：
```
用户：给我讲个小狐狸的故事吧
AI：好的！从前有一只小狐狸...
[DRAW]一只可爱的白色小狐狸在森林里探险[/DRAW]
（系统自动调用画图API并插入图片）
```

### 场景2：启用AI自主画图 + 自定义引导语

```yaml
预设名称：二次元画师
AI自主画图：✓
画图引导语：画图时请使用二次元动漫插画风格，注重人物表情和动作的生动性，色彩明亮鲜艳，适合做壁纸。优先画人物特写和场景氛围图。
```

**效果**：
- AI可以自主画图
- 引导语注入到系统提示词，影响AI的画图决策
- AI会更倾向于创作二次元风格的人物插画

**系统提示词实际内容**：
```
...（原有系统提示词）...

【画图标记速查】用下面的标记来画画，必须另起一行单独输出：
· 画你自己（雪年）  →  [DRAW:ref=xn]场景动作描述（50字内）[/DRAW]
· 参考用户发的图来画  →  [DRAW:ref=img]场景动作描述[/DRAW]
· 两者都要          →  [DRAW:ref=all]场景动作描述[/DRAW]
· 普通画图          →  [DRAW]场景动作描述[/DRAW]
注意：只说"画好啦""咱画了"而不输出上述标记，对方什么也看不到！
一句回复最多一个标记。画雪年时必须用 ref=xn，系统会自动注入角色外观和立绘参考。

【画图风格引导】画图时请使用二次元动漫插画风格，注重人物表情和动作的生动性，色彩明亮鲜艳，适合做壁纸。优先画人物特写和场景氛围图。
```

### 场景3：禁用AI自主画图

```yaml
预设名称：纯文字助手
AI自主画图：✗
画图引导语：（灰色不可编辑）
```

**效果**：
- 系统提示词中**不会**注入 `[DRAW]` 标记协议
- AI不知道可以使用 `[DRAW]` 标记
- AI只会进行纯文字对话
- 用户仍可以手动使用画图模式（如果全局画图已配置）

---

## 配置指南

### 管理员配置步骤

1. **登录管理后台**
   - 访问管理后台 → AI设置 → 预设管理

2. **编辑现有预设或创建新预设**
   - 展开预设配置面板

3. **启用AI自主画图**
   - 勾选"🎨 AI自主画图"

4. **配置画图引导语（可选但推荐）**
   - 在"AI画图引导语"文本框中输入引导内容
   - 建议包含：
     - 画图风格（二次元/写实/水彩等）
     - 何时画图（描述场景/人物动作等）
     - 画图重点（人物特写/场景氛围/构图等）
   - 例如：
     ```
     你擅长创作精美的二次元插画。当对话涉及场景描述、人物动作、情感表达时，
     可以主动配图。画图风格：细腻的线条、明亮的色彩、生动的表情。
     ```

5. **保存设置**
   - 点击"保存设置"按钮
   - 设置立即生效，无需重启

### 用户使用体验

- 用户选择启用了AI自主画图的预设
- AI在对话中会根据情境自主判断何时需要配图
- AI通过 `[DRAW]` 标记触发画图，系统自动调用画图API
- 生成的图片自动插入到对话中

---

## 技术细节

### 系统提示词注入优先级

1. **用户自定义系统提示词**（需预设允许）
2. **预设的系统提示词**
3. **默认系统提示词**
4. **画图标记协议**（仅在预设启用AI自主画图时注入）
   - 基础协议说明
   - 自定义画图引导语（如果配置了）

### 画图标记协议

AI在回复中输出以下格式的标记：
```
[DRAW]画面描述[/DRAW]
```

系统会：
1. 从AI回复中提取 `[DRAW]` 标记
2. 调用画图API生成图片
3. 将生成的图片作为AI消息插入到对话中
4. 从显示的文本中移除 `[DRAW]` 标记

### 兼容性

- **向后兼容**：未配置AI自主画图的预设不受影响
- **全局画图兜底**：如果预设未启用但全局画图已配置，会使用全局配置（兜底逻辑）
- **现有预设**：默认不启用AI自主画图（`supportsImageGen: false`）

---

## 与手动画图模式的区别

| 特性 | AI自主画图 | 手动画图模式 |
|------|-----------|------------|
| **触发方式** | AI在对话中自主判断 | 用户手动点击画图按钮 |
| **控制者** | AI决定何时画、画什么 | 用户输入画面描述 |
| **配置位置** | 预设级别开关 | 全局画图配置 |
| **系统提示词** | 注入画图标记协议 | 不注入 |
| **使用场景** | 让AI更智能、更主动 | 用户明确想要生成图片 |

---

## 最佳实践

### 1. 画图引导语编写建议

**好的引导语**：
```
你是一位专业的插画师。当对话涉及以下情况时可以配图：
1. 描述场景、人物动作、表情
2. 讲故事需要氛围渲染
3. 用户明确要求看图

画图风格：二次元动漫风格，明亮色彩，注重细节和表情。
画面构成：优先人物特写，其次场景氛围图。
```

**避免的写法**：
- 太简短：`二次元风格` ❌
- 太冗长：超过500字的详细指南 ❌
- 强制画图：`每次回复都必须画图` ❌

### 2. 预设配置建议

- **对话助手预设**：建议启用，增强互动性
- **纯文字分析预设**：建议禁用，保持专注
- **创意写作预设**：建议启用+配置插画风格引导
- **技术问答预设**：建议禁用，减少不必要的配图

### 3. 性能考虑

- 画图API调用耗时约30秒
- 建议在引导语中说明"一次回复最多一个标记"
- 避免引导AI过度频繁画图

---

## 故障排查

### AI没有自主画图

1. **检查预设配置**
   - 确认"🎨 AI自主画图"已勾选
   - 保存设置后刷新聊天页面

2. **检查全局画图配置**
   - 管理后台 → AI设置 → AI画图
   - 确认已配置API Key

3. **检查系统日志**
   - 管理后台 → 系统日志
   - 查看画图API调用是否成功

### AI画图风格不符合预期

1. **优化引导语**
   - 使用更明确的风格描述
   - 增加示例和具体要求

2. **检查画图提示词**
   - 引导语会追加到系统提示词
   - 确保语义清晰、不冲突

---

## 相关文件清单

### 服务端
- `server/utils/settings.ts` - 类型定义
- `server/utils/ai.ts` - 系统提示词注入逻辑
- `server/api/presets.get.ts` - 预设列表API

### 前端
- `app/types/index.ts` - 前端类型定义
- `app/components/admin/AdminAISettings.vue` - 管理界面

### 文档
- `CHANGELOG_PRESET_IMAGE_GEN.md` - 本文档

---

## 详细修改

### 1. 服务端类型定义更新

#### 文件：`server/utils/settings.ts`

**新增字段到 `AISettingsPreset` 接口**：
- `supportsImageGen?: boolean` - 是否支持画图功能
- `imageGenPrompt?: string` - 自定义画图提示词前缀

**功能说明**：
- 每个预设可以独立启用/禁用画图功能
- 可为每个预设配置专属的画图提示词前缀
- 提示词前缀会自动添加到用户输入之前，用于引导画图风格

---

### 2. 管理后台界面更新

#### 文件：`app/components/admin/AdminAISettings.vue`

**新增UI元素**：

1. **预设能力复选框区域**：
   - 添加"🎨 支持画图功能"复选框
   - 启用后显示画图提示词输入框

2. **画图提示词配置区域**：
   - 仅在启用画图功能时显示
   - 多行文本框，用于输入自定义提示词前缀
   - 占位符提示：`在用户提示词前添加的引导语，例如：二次元插画风格，高质量，4K分辨率`
   - 提示说明：留空则直接使用用户的原始提示词

3. **预设列表折叠头部**：
   - 在能力图标区域添加 🎨 图标
   - 启用画图功能的预设会显示该图标

**类型定义更新**：
- `PresetForm` 接口新增 `supportsImageGen` 和 `imageGenPrompt` 字段
- 数据加载、新增预设、保存预设时均包含这两个字段

---

### 3. 前端类型定义更新

#### 文件：`app/types/index.ts`

**`ChatPreset` 接口新增字段**：
- `supportsImageGen?: boolean` - 是否支持画图功能
- `imageGenPrompt?: string` - 画图提示词前缀

---

### 4. 预设API更新

#### 文件：`server/api/presets.get.ts`

**返回数据包含画图配置**：
- 每个预设的响应体中包含 `supportsImageGen` 和 `imageGenPrompt`
- 前端可根据这些字段决定是否显示画图入口和如何处理提示词

---

### 5. 画图服务端逻辑更新

#### 文件：`server/utils/ai.ts`

**`generateImage` 函数签名更新**：
```typescript
export async function generateImage(
  prompt: string, 
  referenceImages?: string[], 
  presetName?: string  // 新增：预设名称参数
): Promise<{ b64Json: string }>
```

**新增逻辑**：
1. 接收可选的 `presetName` 参数
2. 根据预设名称查找对应的预设配置
3. 如果预设启用了画图功能且配置了自定义提示词，则提取提示词前缀
4. 将自定义提示词前缀添加到用户提示词之前
5. 组合后的完整提示词发送给画图API

**提示词组合逻辑**：
- 无参考图：`{自定义前缀}{用户提示词}`
- 有参考图：`{角色描述}\n【画面描述】{自定义前缀}{用户提示词}\n注意：...`

#### 文件：`server/api/chat/draw.post.ts`

**新增请求参数**：
- `preset?: string` - 预设名称，用于获取自定义画图提示词
- 从请求体中提取预设名称并传递给 `generateImage`

---

### 6. 前端画图调用更新

#### 文件：`app/composables/useChat.ts`

**`requestDrawUrl` 函数签名更新**：
```typescript
async function requestDrawUrl(
  prompt: string,
  sessionId?: string,
  referenceImages?: string[],
  presetName?: string  // 新增：预设名称参数
): Promise<string | null>
```

**调用更新**：
- 手动画图（`drawImage`）：传递当前会话的预设名称 `session.preset`
- 智能体自主画图（`executeAutoDraws`）：传递当前会话的预设名称 `session.preset`
- 请求体包含 `preset` 字段

---

## 使用场景示例

### 场景1：二次元风格预设
```
预设名称：二次元助手
启用画图：✓
画图提示词：二次元插画风格，高质量，细腻的线条，丰富的色彩，4K分辨率
```

用户输入：`一只可爱的小狐狸`

实际发送给AI：`二次元插画风格，高质量，细腻的线条，丰富的色彩，4K分辨率
一只可爱的小狐狸`

### 场景2：写实风格预设
```
预设名称：写实助手
启用画图：✓
画图提示词：超写实摄影风格，专业相机拍摄，自然光照，高清细节
```

用户输入：`森林中的小溪`

实际发送给AI：`超写实摄影风格，专业相机拍摄，自然光照，高清细节
森林中的小溪`

### 场景3：无自定义提示词
```
预设名称：通用助手
启用画图：✓
画图提示词：（留空）
```

用户输入：`夕阳下的城市`

实际发送给AI：`夕阳下的城市`（原样传递）

---

## 配置指南

### 管理员配置步骤

1. **登录管理后台**
   - 访问管理后台 → AI设置 → 预设管理

2. **编辑现有预设或创建新预设**
   - 展开预设配置面板

3. **启用画图功能**
   - 勾选"🎨 支持画图功能"

4. **配置画图提示词（可选）**
   - 在"画图提示词前缀"文本框中输入引导语
   - 例如：`水彩画风格，柔和的色调，艺术感`

5. **保存设置**
   - 点击"保存设置"按钮
   - 设置立即生效，无需重启

### 用户使用

- 用户在聊天页选择启用了画图功能的预设
- 进入画图模式后输入画面描述
- 系统自动应用该预设的画图提示词前缀
- 生成符合预设风格的图片

---

## 技术细节

### 提示词优先级

1. 角色视觉描述（仅在有参考图时）
2. 预设自定义提示词前缀
3. 用户输入的画面描述
4. 引导语（仅在有参考图时）

### 兼容性

- 向后兼容：未配置画图功能的预设不受影响
- 现有预设：默认不启用画图功能（`supportsImageGen: false`）
- 全局画图配置：保持不变，作为基础功能开关

### 安全考虑

- 画图提示词前缀仅在服务端处理
- 不会暴露在客户端日志中
- 与现有的CSRF保护和限速机制配合使用

---

## 后续优化建议

1. **提示词模板库**
   - 提供常见风格的提示词模板供选择
   - 例如：二次元、写实、水彩、油画等

2. **预览功能**
   - 在配置界面提供提示词效果预览
   - 展示实际组合后的完整提示词

3. **多语言支持**
   - 支持中英文混合提示词
   - 自动优化提示词顺序

4. **统计功能**
   - 统计不同预设的画图使用次数
   - 分析哪些提示词效果最好

---

## 相关文件清单

### 服务端
- `server/utils/settings.ts` - 类型定义
- `server/utils/ai.ts` - 画图逻辑
- `server/api/chat/draw.post.ts` - 画图API端点
- `server/api/presets.get.ts` - 预设列表API

### 前端
- `app/types/index.ts` - 前端类型定义
- `app/composables/useChat.ts` - 聊天逻辑
- `app/components/admin/AdminAISettings.vue` - 管理界面

### 文档
- `CHANGELOG_PRESET_IMAGE_GEN.md` - 本文档
