# 🏠 首页炫酷动效说明

## 已上线的非线性动效

---

## ✨ 动效清单

### 1. 横向视差滚动文字 ParallaxText
**组件**：`ParallaxText.vue`

**效果说明**：
- 超大号文字作为区块背景，随页面滚动产生横向位移
- 不同区块使用不同速度和方向，营造层次感
- 文字透明度很低（3%），仅作装饰不抢注意力

**使用位置**：
- 关于我区块："WELCOME"（向右滚动）
- 博客区块："BLOG"（向左滚动）
- 画廊区块："ARTWORKS"（向右滚动）
- 功能导航区块："EXPLORE"（向左滚动）

**技术实现**：
```
scrollY * speed * direction
```

---

### 2. 遮罩揭示效果 MaskReveal
**组件**：`MaskReveal.vue`

**效果说明**：
- 内容进入视口时，通过动态遮罩从左向右逐渐显现
- 使用 CSS `mask-image` + `mask-position` 实现
- 支持延迟触发和不同方向

**使用位置**：
- 关于我简介内容

**视觉效果**：
- 像帘子一样从左向右拉开
- 非线性缓动曲线，先快后慢

---

### 3. 镜面破碎翻转效果 ImageShatter
**组件**：`ImageShatter.vue`

**效果说明**：
- 图片进入视口时，由12片不规则玻璃碎片从四散飞旋的状态聚合归位
- 每片碎片带独立的3D翻转动画（X轴或Y轴翻转）
- 碎片形状不规则，模拟真实的镜面碎裂
- 归位瞬间有裂纹闪光迸发效果
- 每片碎片带镜面反射高光，模拟玻璃质感

**使用位置**：
- 画廊预览区的6张图片

**技术细节**：
- 12片不规则多边形碎片（`clip-path: polygon()`）
- 每个碎片独立：位置、旋转、翻转轴(X/Y)、翻转角度、延迟
- 3D透视翻转效果（`perspective(800px) rotate3d()`）
- 镜面反射高光层（`mix-blend-mode: overlay`）
- 归位瞬间裂纹闪光迸发动画
- 带回弹的弹性缓动曲线（`cubic-bezier(0.19, 1.4, 0.28, 1)`）

---

### 4. 渐变流动文字 GradientText
**组件**：`GradientText.vue`

**效果说明**：
- 文字使用多色渐变作为填充
- 渐变背景在文字中缓慢流动
- 营造流光溢彩的效果

**渐变色彩**：
- 主色 → 紫色 → 粉色 → 主色 → 紫色
- 6秒一个循环，平滑往复

---

## 🎨 设计理念

### 非线性动效原则

1. **速度非均匀** - 使用弹性缓动（cubic-bezier），而非线性匀速
2. **错落有致** - 卡片/碎片交错延迟入场，形成节奏
3. **多维运动** - 同时包含位移、旋转、缩放、透明度变化
4. **跟随滚动** - 视差效果让页面有深度感

### 性能考量

- ✅ 使用 `will-change` 提示浏览器优化
- ✅ 仅使用 transform 和 opacity 属性（GPU加速）
- ✅ IntersectionObserver 仅在元素可见时触发动画
- ✅ 尊重 `prefers-reduced-motion`，为无障碍用户降级
- ✅ 图片懒加载，避免首屏性能问题

---

## 📱 响应式适配

### 移动端
- 漂浮装饰隐藏，避免与正文争夺注意力
- 破碎效果仍然保留（图片本身的动效）
- 视差文字自动适应字体大小

### 无障碍
- 所有装饰性内容标注 `aria-hidden`
- 尊重系统「减少动画」设置
- 动画失败时内容仍然可见

---

## 🧩 组件文件位置

```
app/components/Home/
├── ParallaxText.vue      # 横向视差文字
├── MaskReveal.vue        # 遮罩揭示效果
├── ImageShatter.vue      # 图片镜面破碎
├── GradientText.vue      # 渐变流动文字
├── SplitReveal.vue       # 分裂揭示（备用）
├── ShatterEffect.vue     # 通用破碎（备用）
└── ScrollParallaxCard.vue # 滚动视差卡片（备用）
```

---

## 🚀 部署状态

✅ **已上线** - xuenian.hellofurry.cn

**验证结果**：
- ✅ ParallaxText 视差文字已生效
- ✅ MaskReveal 遮罩揭示已生效
- ✅ ImageShatter 图片破碎已生效
- ✅ 所有构建通过，无错误

---

## 💡 后续可扩展

1. **鼠标跟随3D倾斜** - 卡片随鼠标位置产生透视倾斜
2. **磁吸按钮** - 鼠标靠近时按钮产生吸附效果
3. **文字逐字入场** - 标题文字逐个字弹入
4. **背景粒子互动** - 粒子跟随鼠标移动
5. **滚动进度条** - 顶部显示阅读进度
6. **数字滚动动画** - 统计数字从0滚动到目标值
