# 管理后台对话历史优化 - 修改说明

## 修改日期
2026-08-16

## 修改内容

### 1. 对话历史服务端改为永久保存，手动清理

#### 修改文件：`server/utils/chat-storage.ts`

- **禁用自动过期时间**：将 `MAX_AGE_MS` 从 7 天改为 `Infinity`（永久保存）
- **禁用定时清理**：将 `CLEANUP_INTERVAL_MS` 改为 `Infinity`
- **简化清理逻辑**：`cleanupUserData()` 函数仅保留空会话清理，不再按时间过期
- **禁用自动清理任务**：`cleanupAllExpiredChats()` 改为空函数
- **更新定时器启动**：`startChatCleanupTimer()` 改为仅输出日志，不启动定时器

#### 修改文件：`server/plugins/chat-cleanup.ts`

- 更新启动日志，说明已切换到永久保存模式

**效果**：
- 所有用户的对话历史将永久保存在服务器
- 不再有 7 天自动清理机制
- 管理员可以在管理后台手动删除用户的对话记录

---

### 2. 用户删除对话仅本地删除，不会同步删除服务器保存

#### 修改文件：`app/composables/useChat.ts`

- **修改 `deleteSession()` 函数**：移除了删除服务端历史的 API 调用
- 用户在前端删除对话时，仅从本地 localStorage 中删除
- 服务端的对话历史保持不变，管理员可以在后台查看

**效果**：
- 用户删除对话后，前端界面不再显示该对话
- 服务器端仍保留完整的对话历史记录
- 管理员可以通过管理后台查看用户已删除的对话

---

### 3. 管理后台查看历史对话增加对图片显示的支持

#### 修改文件：`app/components/admin/AdminChat.vue`

**HTML 结构调整**：
- 将消息内容区域重构为 `detail-msg-body` 容器
- 添加 `detail-msg-images` 容器用于显示图片
- 通过 `msg.parts` 过滤出所有 `image_url` 类型的图片并渲染

**JavaScript 逻辑**：
- 添加 `handleImageError()` 函数处理图片加载失败的情况

**CSS 样式**：
- `.detail-msg-body`：使用 flexbox 纵向排列文本和图片
- `.detail-msg-images`：flexbox 横向排列，支持换行
- `.detail-msg-image`：
  - 限制最大宽高为 200px
  - 添加圆角和 hover 放大效果
  - 使用 `object-fit: cover` 保持图片比例

**效果**：
- 管理后台查看对话详情时，可以看到消息中包含的图片
- 图片以缩略图形式展示，hover 时略微放大
- 支持多张图片横向排列
- 图片加载失败时自动隐藏

---

## 技术细节

### 图片 URL 支持格式
- Base64 data URL（`data:image/...`）：用户刚上传的图片
- 服务端相对路径（`/images/chat/...`）：已持久化的历史图片

### 安全考虑
- 图片加载失败时优雅降级（隐藏图片而非显示错误图标）
- 服务端路径仅限于 `/images/` 目录，有路径遍历防护

---

## 管理员使用说明

1. **查看对话历史**：
   - 登录管理后台 → 聊天管理
   - 用户列表中点击"查看"按钮查看详情
   - 详情页面会显示文本消息和图片消息

2. **手动清理对话**：
   - 在用户列表中点击"删除"按钮
   - 确认后将删除该用户的所有对话记录（不可恢复）
   - 这是目前唯一的清理方式（不再有自动过期）

3. **注意事项**：
   - 对话历史永久保存，需定期检查存储空间
   - 用户在前端删除的对话仍会保存在服务器
   - 建议根据实际需求定期清理长期不活跃的用户记录

---

## 兼容性说明

- 本次修改向后兼容，不影响现有数据
- 已存储的历史对话会继续保留（不会因为修改而被清理）
- 前端删除对话的操作不会影响已有的服务端数据
